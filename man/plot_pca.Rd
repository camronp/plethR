% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pca.R
\name{plot_pca}
\alias{plot_pca}
\title{Create PCA Plot}
\usage{
plot_pca(
  data,
  group_mapping = NULL,
  parameters = NULL,
  use_clustering = FALSE,
  num_clusters = 4,
  color_by = c("group", "cluster"),
  show_ellipses = TRUE,
  ellipse_type = "norm",
  ellipse_level = 0.95,
  show_labels = TRUE,
  label_size = 3,
  point_size = 3,
  components = c("PC1", "PC2"),
  show_variance = TRUE,
  show_loadings = FALSE,
  n_loadings = 5,
  color_palette = "Set1",
  random_seed = 123,
  save_plot = FALSE,
  output_dir = "figures/pca",
  file_prefix = "pca",
  width = 8,
  height = 6,
  dpi = 300
)
}
\arguments{
\item{data}{A list of data frames from `sheets_into_list()`, where each element
is one subject/mouse. Each data frame should contain time series respiratory data.}

\item{group_mapping}{Named list from `assign_groups_interactive()` mapping groups
to sheet indices. If `NULL`, each subject is its own group. Default is `NULL`.}

\item{parameters}{Character vector of parameters to include in PCA. If `NULL`, uses all
numeric columns. Default is `NULL`.}

\item{use_clustering}{Logical; if `TRUE`, applies k-means clustering. Default is `FALSE`.}

\item{num_clusters}{Integer specifying number of k-means clusters. Default is 4.}

\item{color_by}{Character string specifying coloring.
Options: "group" (color by experimental group, default) or "cluster" (color by k-means cluster).}

\item{show_ellipses}{Logical; if `TRUE`, shows confidence ellipses around groups. Default is `TRUE`.}

\item{ellipse_type}{Character string specifying ellipse type.
Options: "norm", "t", or "euclid". Default is "norm".}

\item{ellipse_level}{Numeric confidence level (0-1). Default is 0.95.}

\item{show_labels}{Logical; if `TRUE`, displays subject labels. Default is `TRUE`.}

\item{label_size}{Numeric specifying label text size. Default is 3.}

\item{point_size}{Numeric specifying point size. Default is 3.}

\item{components}{Character vector of PCs to plot. Default is c("PC1", "PC2").}

\item{show_variance}{Logical; if `TRUE`, shows variance percentage in axis labels. Default is `TRUE`.}

\item{show_loadings}{Logical; if `TRUE`, adds loading vectors. Default is `FALSE`.}

\item{n_loadings}{Integer number of loading vectors to show. Default is 5.}

\item{color_palette}{Character string specifying color scheme.
Options: "Set1" (default), "Set2", "Dark2", "viridis", etc.}

\item{random_seed}{Integer for k-means reproducibility. Default is 123.}

\item{save_plot}{Logical; if `TRUE`, saves plot to file. Default is `FALSE`.}

\item{output_dir}{Character string specifying output directory. Default is "figures/pca".}

\item{file_prefix}{Character string to prepend to filename. Default is "pca".}

\item{width}{Numeric specifying plot width in inches. Default is 8.}

\item{height}{Numeric specifying plot height in inches. Default is 6.}

\item{dpi}{Numeric specifying resolution for saved plots. Default is 300.}
}
\value{
A list containing: plot (ggplot object), pca (prcomp object), variance (data frame),
  clusters (k-means results if used, NULL otherwise), and scores (PC scores with metadata).
}
\description{
Performs PCA on individual subjects, with each point representing one subject's
average respiratory parameters across time.
}
\examples{
\dontrun{
# Load data
df_list <- sheets_into_list("data.xlsx", clean_time = TRUE)

# Define groups
groups <- set_group_names("Control", "Low", "Medium", "High")
mapping <- assign_groups_interactive(groups, df_list)

# PCA colored by group (16 mice, 4 groups)
result <- plot_pca(df_list, group_mapping = mapping)
result$plot

# Check variance explained
print(result$variance)

# With clustering
result <- plot_pca(df_list,
                   group_mapping = mapping,
                   use_clustering = TRUE,
                   num_clusters = 4)

# Show which parameters drive separation
result <- plot_pca(df_list,
                   group_mapping = mapping,
                   show_loadings = TRUE,
                   parameters = c("f", "TVb", "MVb", "Penh"))
}

}
